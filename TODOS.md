# TODOS

## Django 서버(단일 Postgres 인스턴스)의 SPOF 대응 — 백업/HA 정책

**What:** 모니터링 플랫폼의 중앙 서버(Django + 단일 PostgreSQL)에 백업 및 고가용성(HA) 정책을 추가한다.

**Why:** 현재 설계(plan.md, dyhwang-agent-design-20260723-113429.md)는 서버 1대 + DB 1대 구조다. 서버가 죽거나 DB가 손상되면 전체 호스트 함대의 모니터링 가시성이 동시에 사라진다. Go 에이전트의 로컬 재시도 큐는 "데이터 유실"은 막아주지만 "서버가 살아있어야 대시보드를 볼 수 있다"는 가용성 문제 자체는 해결하지 못한다.

**Pros:** 서버 장애 시에도 과거 이력 조회·알림이 계속 동작. 실제 여러 조직/서버를 운영하는 단계에 들어가면 이게 없으면 신뢰할 수 없는 도구가 된다.

**Cons:** Phase 1(호스트 1대 PoC) 단계에서는 완전한 과잉 설계 — DB 복제, 장애 조치(failover) 자동화 등은 아직 쓸 일 없는 인프라를 미리 짓는 것.

**Context:** Phase 1~5는 단일 인스턴스를 전제로 설계되어 있다(plan.md §3-2, §4-1). 여러 서버를 실제로 온보딩하기 시작하는 시점, 또는 Phase 6(운영 강화) 착수 시 재검토. 후보: PostgreSQL 정기 백업(pg_dump/WAL 아카이빙) 먼저, 이후 필요하면 read replica.

**Depends on:** Phase 1~5 완료 후, 실제 다중 호스트 운영이 시작될 때.

---
